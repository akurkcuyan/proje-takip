/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/projetakip', // Yayında (/projetakip klasöründe) çalışması için aktif edildi
};

export default nextConfig;
